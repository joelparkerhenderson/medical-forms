-- ============================================================
-- 01_patient.sql
-- Root entity: a unique individual in the system.
-- ============================================================
-- A patient can have many satisfaction survey records over time
-- (e.g. for different encounters). Identity data lives here;
-- survey data lives in the encounter_satisfaction tables.
-- ============================================================

CREATE TABLE patient (
    -- Primary key: UUIDv4 generated by the database
    id                    UUID PRIMARY KEY DEFAULT gen_random_uuid(),

    -- Patient identity
    first_name            TEXT NOT NULL DEFAULT '',
    last_name             TEXT NOT NULL DEFAULT '',
    date_of_birth         DATE,

    -- Biological sex
    sex                   TEXT NOT NULL DEFAULT ''
                          CHECK (sex IN ('male', 'female', 'other', '')),

    -- Audit timestamps
    created_at            TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at            TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Auto-update updated_at on every row change
CREATE TRIGGER trg_patient_updated_at
    BEFORE UPDATE ON patient
    FOR EACH ROW EXECUTE FUNCTION set_updated_at();

COMMENT ON TABLE patient IS
    'Root entity representing a unique individual. Identity data only; survey data is per encounter_satisfaction.';
COMMENT ON COLUMN patient.id IS
    'UUIDv4 primary key, auto-generated.';
COMMENT ON COLUMN patient.first_name IS
    'Patient given name.';
COMMENT ON COLUMN patient.last_name IS
    'Patient family name.';
COMMENT ON COLUMN patient.date_of_birth IS
    'Date of birth. NULL if not recorded.';
COMMENT ON COLUMN patient.sex IS
    'Biological sex: male, female, other, or empty string if unanswered.';
COMMENT ON COLUMN patient.created_at IS
    'Row creation timestamp.';
COMMENT ON COLUMN patient.updated_at IS
    'Last modification timestamp, auto-updated by trigger.';
