# Dashboard — static HTML

Static HTML + Alpine.js dashboard listing DASS-21 psychology assessments
with subscale severities and risk flags. Read-only review view for
clinicians.

Open `index.html` directly or serve with `python3 -m http.server 8080`.
