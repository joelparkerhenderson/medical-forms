# Dashboard — static HTML

Static HTML + Alpine.js dashboard listing recent patient-completed
pre-operative assessments with the computed ASA grade and key safety
flags. Read-only review view for an anaesthetic pre-assessment team.

Open `index.html` directly or serve with `python3 -m http.server 8080`.
